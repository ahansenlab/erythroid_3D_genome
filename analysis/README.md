## Analysis Files 

This contains the core code used to analyze processed data. 

1. python_scripts: execution of analyses in python
2. bash_scripts: bash scripts for motif calling, aggregate peak analyses, and intersection scripts to generate the CRE classes used in the paper.
3. modules: common functions that are called in the python scripts as well as in the figure generation notebooks.
